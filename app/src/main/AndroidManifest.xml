<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.amicablesoft.polygonereporter"
          xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Polygon.Theme"
        tools:ignore="GoogleAppIndexingWarning">

        <activity
            android:name=".ui.EntryActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Polygon.Theme.NoActionBar">
        <intent-filter>
            <action android:name="android.intent.action.MAIN"/>

            <category android:name="android.intent.category.LAUNCHER"/>
        </intent-filter>
    </activity>

        <activity
            android:name=".ui.MainActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Polygon.Theme.NoActionBar">
        </activity>

        <activity
            android:name=".ui.ReportActivity"
            android:screenOrientation="portrait"
            android:label="@string/report__title">
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${file_provider_authorities}"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_path" />
        </provider>

        <meta-data
            android:name="com.google.android.maps.v2.API_KEY"
            android:value="AIzaSyDENNkA6qbkQMGbw_lVAXc8ejrQZzkv8Os"/>
    </application>

</manifest>